[tool.poetry]
name = "price-tracker"
version = "0.1.0"
description = ""
authors = ["fewexc1, bRAVE"]
readme = "README.md"
packages = [{ include = "app", from = "src" }]

[tool.poetry.dependencies]
python = ">=3.12,<3.15"
aiogram = "^3.28.2"
python-dotenv = "^1.2.2"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = ["pytest (>=9.0.3,<10.0.0)"]
